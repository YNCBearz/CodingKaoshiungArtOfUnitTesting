pipeline {
    agent { docker { image 'php' } }
    stages {
        stage('Composer Install') {
            steps {
                echo 'Installing Vendor'
                sh 'composer install'
            }
        }

        stage('PHPUnit') {
            steps {
                sh './vendor/bin/phpunit'
            }
        }
    }
}